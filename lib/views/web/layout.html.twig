<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Crypto Currencies</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
<header>
    <h1>Currency Tokens</h1>
</header>

<main>
    {% block favourites %}    
    {% endblock %}   

<section class="section-main list">
    <section class="section-title">
        <i class="fa-solid fa-list"></i>
        <h2>Tokens</h2>
    </section>
    <form action="show-price.php" method="get" class="section-select-tokens">
        <label id="dropdown_token_from">from</label>
        <select name="dropdown_token_from">
            {% for item in items %}
                <option {% if item == token_from %}selected{% endif %}>{{ item }}</option>
            {% endfor %}
        </select>
        <label id="dropdown_token_to">to</label>
        <select name="dropdown_token_to">
            {% for item in items %}
                <option {% if item == token_to %}selected{% endif %}>{{ item }}</option>
            {% endfor %}
        </select>

        <button class="btn-price"><i class="fa-solid fa-magnifying-glass"></i>Show Price</button>
    </form>
    {% block price %}
    {% endblock %}
    {% block error %}
    {% endblock %}
</section>
</main>

<footer>
    <p class="text-footer">Project created in 2024</p>
</footer>
</body>
</html>